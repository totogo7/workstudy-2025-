好 —— 我按照你要求的阶段推进，把**下一步（阶段 4：用户与认证模块）**完整落地：从依赖到代码、配置、数据库补充、运行验证、前端对接示例、以及安全/优化说明，全给你了。你按我写的文件路径把文件放到 `library-backend` 项目里，启动就能跑通登录/注册/鉴权流程并用 Redis 做 session 缓存与登出失效。

> 说明：我选的是 **用户注册 + 登录（JWT）+ Redis 会话缓存 + 拦截器鉴权** 这一块（这是阶段 4 的核心）。如果你想我接着做“图书 CRUD”或“并发借书”我马上继续。

------

## 一、先把 `pom.xml` 的依赖补全（新增 JWT + bcrypt 支持）

编辑 `library-backend/pom.xml`，在 `<dependencies>` 中追加以下依赖（如果你已经有了某些，请合并）：

```xml
<!-- JWT -->
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt</artifactId>
  <version>0.9.1</version>
</dependency>

<!-- Spring Security Crypto（BCrypt）-->
<dependency>
  <groupId>org.springframework.security</groupId>
  <artifactId>spring-security-crypto</artifactId>
</dependency>

<!-- Jackson (if not present) -->
<dependency>
  <groupId>com.fasterxml.jackson.core</groupId>
  <artifactId>jackson-databind</artifactId>
</dependency>
```

保存后在 IDEA 执行 `mvn -U clean package` 或刷新 Maven 依赖。

------

## 二、配置（application.yml）

在 `src/main/resources/application.yml` 加入/确认以下配置（我把 JWT secret 和 Redis TTL 放这里）：

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: libuser
    password: libpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379

library:
  jwt:
    secret: my_super_secret_change_this  # 面试/开发环境可用，生产请放 env/secret manager
    expire-ms: 7200000                   # 2 hours
    redis-key-prefix: login:
    refresh-expire-ms: 172800000         # refresh token TTL 2 days (可选)
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
```

> 提示：`library.jwt.secret` 生产不要放明文到代码库，用环境变量或 secrets 管理。

------

## 三、数据库支持：增加 password 字段已存在（V1 已建），但补充 update SQL

我们之前的 Flyway V1 已创建 `users`，但为了演示安全注册，我给你新增一个 V2 migration（用于 dev 环境）来增加索引或把 seed password 处理（可选）。
 创建文件：`library-backend/src/main/resources/db/migration/V2__ensure_users_index.sql`：

```sql
-- V2__ensure_users_index.sql
ALTER TABLE users
  ADD INDEX idx_users_username (username);
```

（如果 Flyway 已执行 V1，重启时会执行 V2 自动添加索引。）

如果你想把种子 `admin` 密码改成 BCrypt，你可以手工在 DB 用 Java 程序或脚本更新，后面我会在注册逻辑中兼容 MD5->BCrypt 迁移（便于保留 seed 用户）。

------

## 四、后端代码（按路径放置）

下面一并给出所有需要添加/修改的 Java 源文件。包名全部以 `com.library` 开头，与你现有结构一致。

------

### 1) DTOs：`AuthRequest` / `AuthResponse` / `RegisterRequest`

创建 `library-backend/src/main/java/com/library/dto/RegisterRequest.java`：

```java
package com.library.dto;

import lombok.Data;

@Data
public class RegisterRequest {
    private String username;
    private String password;
}
```

创建 `library-backend/src/main/java/com/library/dto/AuthRequest.java`:

```java
package com.library.dto;

import lombok.Data;

@Data
public class AuthRequest {
    private String username;
    private String password;
}
```

创建 `library-backend/src/main/java/com/library/dto/AuthResponse.java`:

```java
package com.library.dto;

import lombok.AllArgsConstructor;
import lombok.Data;

@Data
@AllArgsConstructor
public class AuthResponse {
    private String token;
    private String username;
}
```

------

### 2) 更新 `mapper/UserMapper.java`（增加 insert/update）

修改或替换 `library-backend/src/main/java/com/library/mapper/UserMapper.java` 为：

```java
package com.library.mapper;

import com.library.entity.User;
import org.apache.ibatis.annotations.*;

@Mapper
public interface UserMapper {

    @Select("SELECT id, username, password, role FROM users WHERE username = #{username}")
    User findByUsername(String username);

    @Insert("INSERT INTO users(username, password, role) VALUES(#{username}, #{password}, #{role})")
    @Options(useGeneratedKeys = true, keyProperty = "id")
    int insert(User user);

    @Update("UPDATE users SET password = #{password} WHERE id = #{id}")
    int updatePassword(@Param("id") Long id, @Param("password") String password);
}
```

------

### 3) JwtUtil（生成/解析 Token）

新建 `library-backend/src/main/java/com/library/util/JwtUtil.java`：

```java
package com.library.util;

import io.jsonwebtoken.*;
import java.util.Date;
import java.util.Map;
import java.util.HashMap;

public class JwtUtil {
    private static String SECRET;
    private static long EXPIRE_MS;

    public static void init(String secret, long expireMs) {
        SECRET = secret;
        EXPIRE_MS = expireMs;
    }

    public static String generateToken(Long userId, String username, String role) {
        Date now = new Date();
        Date exp = new Date(now.getTime() + EXPIRE_MS);
        Map<String, Object> claims = new HashMap<>();
        claims.put("id", userId);
        claims.put("username", username);
        claims.put("role", role);
        return Jwts.builder()
                .setClaims(claims)
                .setSubject(String.valueOf(userId))
                .setIssuedAt(now)
                .setExpiration(exp)
                .signWith(SignatureAlgorithm.HS256, SECRET)
                .compact();
    }

    public static Claims parseToken(String token) {
        return Jwts.parser().setSigningKey(SECRET).parseClaimsJws(token).getBody();
    }
}
```

> 我们会在配置类启动时用 `JwtUtil.init(...)` 初始化 SECRET 与 EXPIRE。

------

### 4) Redis 配置 Bean（RedisTemplate） & Jwt init

新建 `library-backend/src/main/java/com/library/config/RedisConfig.java`：

```java
package com.library.config;

import com.library.util.JwtUtil;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Bean;
import org.springframework.data.redis.connection.lettuce.LettuceConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;

@Configuration
public class RedisConfig {

    @Value("${spring.redis.host}")
    private String redisHost;
    @Value("${spring.redis.port}")
    private int redisPort;

    @Bean
    public LettuceConnectionFactory redisConnectionFactory() {
        return new LettuceConnectionFactory(redisHost, redisPort);
    }

    @Bean
    public RedisTemplate<String, Object> redisTemplate(LettuceConnectionFactory lcf) {
        RedisTemplate<String, Object> rt = new RedisTemplate<>();
        rt.setConnectionFactory(lcf);
        return rt;
    }

    // init JWT static config
    @Bean
    public void initJwt(@Value("${library.jwt.secret}") String secret,
                        @Value("${library.jwt.expire-ms}") long expireMs) {
        JwtUtil.init(secret, expireMs);
    }
}
```

------

### 5) UserService 与实现

创建 interface `library-backend/src/main/java/com/library/service/UserService.java`:

```java
package com.library.service;

import com.library.entity.User;

public interface UserService {
    User findByUsername(String username);
    User register(String username, String password); // returns created user or null if exists
    String login(String username, String password); // returns token or null
    void logout(Long userId);
}
```

实现类 `library-backend/src/main/java/com/library/service/impl/UserServiceImpl.java`:

```java
package com.library.service.impl;

import com.library.entity.User;
import com.library.mapper.UserMapper;
import com.library.service.UserService;
import com.library.util.JwtUtil;
import org.springframework.stereotype.Service;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

import javax.annotation.Resource;
import java.util.concurrent.TimeUnit;
import java.security.MessageDigest;

@Service
public class UserServiceImpl implements UserService {

    @Resource
    private UserMapper userMapper;
    @Resource
    private RedisTemplate<String, Object> redisTemplate;

    private BCryptPasswordEncoder encoder = new BCryptPasswordEncoder();

    @Override
    public User findByUsername(String username) {
        return userMapper.findByUsername(username);
    }

    @Override
    public User register(String username, String password) {
        User exist = userMapper.findByUsername(username);
        if (exist != null) return null;
        String hashed = encoder.encode(password);
        User user = new User();
        user.setUsername(username);
        user.setPassword(hashed);
        user.setRole("user");
        userMapper.insert(user);
        return user;
    }

    @Override
    public String login(String username, String password) {
        User user = userMapper.findByUsername(username);
        if (user == null) return null;
        String dbPwd = user.getPassword();
        boolean ok = false;

        // if dbPwd looks like bcrypt (starts with $2a$ or $2b$), use BCrypt
        if (dbPwd != null && (dbPwd.startsWith("$2a$") || dbPwd.startsWith("$2b$"))) {
            ok = encoder.matches(password, dbPwd);
        } else {
            // legacy MD5 fallback (seed user). if matches, re-hash to bcrypt and update DB
            try {
                MessageDigest md = MessageDigest.getInstance("MD5");
                byte[] digest = md.digest(password.getBytes());
                StringBuilder sb = new StringBuilder();
                for (byte b : digest) sb.append(String.format("%02x", b));
                String md5 = sb.toString();
                if (md5.equalsIgnoreCase(dbPwd)) {
                    ok = true;
                    // re-hash to bcrypt
                    String newHash = encoder.encode(password);
                    userMapper.updatePassword(user.getId(), newHash);
                }
            } catch (Exception e) {
                // ignore
            }
        }

        if (!ok) return null;

        String token = JwtUtil.generateToken(user.getId(), user.getUsername(), user.getRole());
        // store token in redis for quick session validation / immediate logout
        String key = "login:" + user.getId();
        redisTemplate.opsForValue().set(key, token, 2, TimeUnit.HOURS); // TTL align with token
        return token;
    }

    @Override
    public void logout(Long userId) {
        String key = "login:" + userId;
        redisTemplate.delete(key);
    }
}
```

说明：

- 登录会把 token 存到 Redis `login:{userId}`，拦截器会从 Redis 校验，支持主动登出（删除 Redis key）。
- 对 seed MD5 密码支持：若匹配则 migrate 到 BCrypt。

------

### 6) AuthController

创建 `library-backend/src/main/java/com/library/controller/AuthController.java`：

```java
package com.library.controller;

import com.library.common.Result;
import com.library.dto.AuthRequest;
import com.library.dto.AuthResponse;
import com.library.dto.RegisterRequest;
import com.library.entity.User;
import com.library.service.UserService;
import org.springframework.web.bind.annotation.*;

import javax.annotation.Resource;
import java.util.HashMap;
import java.util.Map;

@RestController
@RequestMapping("/api/auth")
public class AuthController {

    @Resource
    private UserService userService;

    @PostMapping("/register")
    public Result<String> register(@RequestBody RegisterRequest req) {
        if (req.getUsername() == null || req.getPassword() == null) {
            return Result.error("username/password required");
        }
        User u = userService.register(req.getUsername(), req.getPassword());
        if (u == null) return Result.error("username exists");
        return Result.success("registered");
    }

    @PostMapping("/login")
    public Result<AuthResponse> login(@RequestBody AuthRequest req) {
        String token = userService.login(req.getUsername(), req.getPassword());
        if (token == null) return Result.error("invalid username/password");
        return Result.success(new AuthResponse(token, req.getUsername()));
    }

    @PostMapping("/logout")
    public Result<String> logout(@RequestParam Long userId) {
        userService.logout(userId);
        return Result.success("logged out");
    }
}
```

> 登录返回的 `Result.data` 是 `AuthResponse` 包含 token 和 username。

------

### 7) Auth Interceptor（校验 JWT + Redis session）

创建 `library-backend/src/main/java/com/library/config/AuthInterceptor.java`：

```java
package com.library.config;

import com.library.util.JwtUtil;
import io.jsonwebtoken.Claims;
import org.springframework.stereotype.Component;
import org.springframework.web.servlet.HandlerInterceptor;
import org.springframework.data.redis.core.RedisTemplate;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@Component
public class AuthInterceptor implements HandlerInterceptor {

    @Resource
    private RedisTemplate<String, Object> redisTemplate;

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response,
                             Object handler) throws Exception {
        String path = request.getRequestURI();
        // allow public endpoints
        if (path.startsWith("/api/auth") || path.equals("/health") || path.startsWith("/swagger")) return true;

        String auth = request.getHeader("Authorization");
        if (auth == null || auth.isEmpty()) {
            response.setStatus(401);
            return false;
        }
        // expect "Bearer token"
        String token = auth.startsWith("Bearer ") ? auth.substring(7) : auth;
        try {
            Claims claims = JwtUtil.parseToken(token);
            Long userId = Long.valueOf(claims.get("id").toString());
            // validate session in redis
            String key = "login:" + userId;
            Object cached = redisTemplate.opsForValue().get(key);
            if (cached == null) {
                response.setStatus(401);
                return false;
            }
            // simple check token equals cached token (optional)
            if (!token.equals(cached.toString())) {
                response.setStatus(401);
                return false;
            }
            // put user info into request attributes for controllers
            request.setAttribute("userId", userId);
            request.setAttribute("username", claims.get("username"));
            request.setAttribute("role", claims.get("role"));
            return true;
        } catch (Exception e) {
            response.setStatus(401);
            return false;
        }
    }
}
```

### 8) Register interceptor in WebMvcConfig

Add file `library-backend/src/main/java/com/library/config/WebMvcConfig.java`:

```java
package com.library.config;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebMvcConfig implements WebMvcConfigurer {

    @Autowired
    private AuthInterceptor authInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(authInterceptor)
                .addPathPatterns("/**")
                .excludePathPatterns("/api/auth/**", "/health", "/swagger-ui.html", "/v2/api-docs", "/swagger-resources/**");
    }
}
```

------

### 9) Add a protected test endpoint (optional) — `UserController`

Create `library-backend/src/main/java/com/library/controller/UserController.java`:

```java
package com.library.controller;

import com.library.common.Result;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import javax.servlet.http.HttpServletRequest;

@RestController
public class UserController {

    @GetMapping("/api/me")
    public Result<?> me(HttpServletRequest req) {
        Object uid = req.getAttribute("userId");
        Object uname = req.getAttribute("username");
        if (uid == null) return Result.error("not authenticated");
        return Result.success("userId:" + uid + ", username:" + uname);
    }
}
```

------

## 五、前端对接示例（更新 Login.vue）

你前面有 Login.vue，我给出更新后的 login 调用地址与 header 格式（Bearer token）。

替换前端 `library-frontend/src/views/Login.vue` 的 login 方法为：

```js
methods: {
  async login() {
    try {
      const res = await axios.post("http://localhost:8080/api/auth/login", {
        username: this.form.username,
        password: this.form.password
      });
      if (res.data.code === 1) {
        const token = res.data.data.token;
        localStorage.setItem("token", token);
        axios.defaults.headers.common['Authorization'] = 'Bearer ' + token;
        this.$router.push("/books");
      } else {
        this.$message.error(res.data.msg);
      }
    } catch (e) {
      this.$message.error("network error");
    }
  }
}
```

在前端项目启动时（main.js）加入自动注入 header（若从 localStorage 读取到 token）：

```js
const token = localStorage.getItem('token');
if (token) {
  axios.defaults.headers.common['Authorization'] = 'Bearer ' + token;
}
```

------

## 六、如何运行 & 验证（step-by-step）

1. 确保已经运行 `docker-compose up -d`（MySQL + Redis + phpMyAdmin）并且 MySQL 可访问。
2. 在 `library-backend` 运行 `mvn -U clean package`，然后 `mvn spring-boot:run` 或使用 IDEA 启动 `LibraryApplication`。 观察控制台 Flyway 执行 V1/V2 脚本。
3. 测试注册（curl/Postman）：

**注册（curl）**

```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"alice123"}'
```

期望返回：

```json
{"code":1,"msg":"success","data":"registered"}
```

**登录（curl）**

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"alice123"}'
```

期望返回（示例）：

```json
{
  "code":1,
  "msg":"success",
  "data": {
    "token":"eyJhbGciOiJIUzI1NiJ9....",
    "username":"alice"
  }
}
```

1. 用 token 访问受保护接口 `/api/me`：

```bash
curl -H "Authorization: Bearer eyJhbGciOi..." http://localhost:8080/api/me
```

期望返回：

```json
{"code":1,"msg":"success","data":"userId:5, username:alice"}
```

1. 登出（示例）：

```bash
curl -X POST "http://localhost:8080/api/auth/logout?userId=5"
```

然后再次访问 `/api/me`：应返回 401（拦截器返回）。

------

## 七、测试要点（你需要写/跑的测试）

- 单元测试 UserService（Mockito mock UserMapper & RedisTemplate）验证：
  - 成功注册（插入调用）；
  - 登录成功时 Redis 存键并返回 token；
  - MD5 legacy 登录时会迁移到 BCrypt（你可以 mock a user with md5 password and test updatePassword called）。
- 集成测试（可选）：`@SpringBootTest` 启动 app + Testcontainers MySQL/Redis，完整执行 register/login/me logout 流程。
- Postman Collection：把 `/api/auth/register`, `/api/auth/login`, `/api/me`, `/api/auth/logout` 加入集合并导出作为 demo。

------

## 八、设计/安全说明 & 优化建议（面试时可以这么说）

1. **为什么 JWT + Redis？**
   - JWT 提供无状态认证；Redis 用来支持“立即登出/单点登出/黑名单”场景（需要强制失效 token），还可以存用户会话元信息，提高校验效率。
2. **密码存储**：使用 BCrypt（`BCryptPasswordEncoder`），生产必备；我在实现中也兼顾了 legacy MD5->BCrypt 的平滑迁移策略。
3. **Token 安全**：短期 access token（2h）；可以引入 Refresh Token 机制（更安全），并使用 HTTPS 强制客户端传输。生产端 SECRET 不放配置文件中，改用环境变量/secret manager。
4. **防滥用**：登录失败次数限制（redis计数+验证码）、IP 限速、密码强度校验。
5. **扩展**：推荐未来把权限系统换成 Spring Security + role/authority 注解控制（细粒度），并接入 OAuth2 / OpenID Connect 若需要 SSO。

------

## 九、常见问题排查（快速参考）

- `JwtUtil.parseToken` 报签名异常：确认 `library.jwt.secret` 与生成时用的一致（init 在 RedisConfig 中执行）。
- `RedisTemplate` 为 null：确认 `LettuceConnectionFactory` 已正确注入；`spring.redis.host`/`port` 指向运行中的 Redis。
- `Flyway` 未执行新脚本：检查 `classpath:db/migration` 路径及文件名以 `V<number>__desc.sql` 格式命名。

------

## 十、下一步建议（你可以选）

现在用户认证模块完成并可演示，你可以继续以下任意一个我来实现下一步（我会把代码＋前端联调示例＋测试一起给你）：

1. **图书 CRUD**（REST API + DTO 校验 + Swagger 注释 + 前端 BookList.vue / BookEdit.vue 对接）。
2. **并发借书流程**（book_copies + borrow_records + 事务 + `SELECT ... FOR UPDATE` + 并发集成测试）。
3. **搜索 / ElasticSearch 接入 demo**（基础 MySQL FULLTEXT 优化先实现，然后演示 ES 索引示例）。
4. **权限系统升级**（用 Spring Security：admin 页面保护 + admin role 封装）。

告诉我你要哪个（建议直接做 2：并发借书 —— 面试非常喜欢），我马上给出完整实现。